package models

object Aliases {
  def u = User.defaultAlias
  def l = Lens.defaultAlias
  def c = Camera.defaultAlias
  def i = Image.defaultAlias
  def cond = Condition.defaultAlias
  def pr = PasswordReset.defaultAlias
  def s = Session.defaultAlias
}
